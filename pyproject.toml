[project]
name = "lab02-github-profiler"
version = "0.1.0"
description = "GitHub API profiler - repository health analysis."
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
    "python-dotenv>=1.1",
]

[project.scripts]
github-profiler = "github_profiler:main"
